name: Deploy 13Xplay Controlled Elementor Landing

on:
  push:
    branches:
      - 13xplay-theme-sync

concurrency:
  group: deploy-13xplay-controlled-editor
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FTP_SERVER: ${{ secrets.FTP_SERVER }}
      FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
      FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare 13Xplay artwork assets
        run: |
          mkdir -p /tmp/x13-assets
          curl --fail --silent --show-error \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/git/blobs/7f851c41d70033654cfcc67df92f78147d3c6641" \
            | jq -r '.content' | tr -d '\n' | base64 -d > /tmp/x13-assets/13xplay-logo.webp
          curl --fail --silent --show-error \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/git/blobs/bdc74f286880483934ea67b3f2cbb96845efd640" \
            | jq -r '.content' | tr -d '\n' | base64 -d > /tmp/x13-assets/13xplay-hero.webp
          test -s /tmp/x13-assets/13xplay-logo.webp
          test -s /tmp/x13-assets/13xplay-hero.webp

      - name: Upload branded artwork
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file /tmp/x13-assets/13xplay-logo.webp \
            "ftp://${FTP_SERVER}/wp-content/uploads/13xplay-assets/13xplay-logo.webp"
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file /tmp/x13-assets/13xplay-hero.webp \
            "ftp://${FTP_SERVER}/wp-content/uploads/13xplay-assets/13xplay-hero.webp"

      - name: Upload persistent controlled Elementor editor
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-controlled-elementor-widget.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-controlled-elementor-widget.php"

      - name: Upload editable footer menu controls
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-footer-menu-controls.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-footer-menu-controls.php"

      - name: Upload persistent mobile reference polish
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-mobile-reference-polish.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-mobile-reference-polish.php"

      - name: Upload mobile poster V2
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-mobile-poster-v2.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-mobile-poster-v2.php"

      - name: Upload larger trust strip icons
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-trust-icon-size.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-trust-icon-size.php"

      - name: Upload desktop bonus badge polish
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-desktop-bonus-polish.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-desktop-bonus-polish.php"

      - name: Upload mobile box cleanup
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-mobile-box-cleanup.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-mobile-box-cleanup.php"

      - name: Upload mobile GET ID card fix
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-mobile-id-card-fix.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-mobile-id-card-fix.php"

      - name: Upload privacy policy page
        run: |
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "tools/x13-privacy-policy.php" \
            "ftp://${FTP_SERVER}/wp-content/mu-plugins/x13-privacy-policy.php"

      - name: Verify premium layout and privacy policy live
        run: |
          sleep 3
          HTML="$(curl --fail --silent --show-error --location --retry 3 --max-time 60 "https://13xplay.upscaleera.com/?cb=${GITHUB_SHA}")"
          echo "$HTML" | grep -E "x13-fixed|x13-main-wa|x13-feature-strip|x13-footer|x13-mobile-reference-polish|x13-mobile-poster-v2|x13-trust-icon-size|x13-desktop-bonus-polish|x13-mobile-box-cleanup|x13-mobile-id-card-fix|data-x13-footer-menu-controls|Privacy Policy" >/dev/null
          POLICY="$(curl --fail --silent --show-error --location --retry 3 --max-time 60 "https://13xplay.upscaleera.com/privacy-policy/?cb=${GITHUB_SHA}")"
          echo "$POLICY" | grep -F "At 13XPlay, we respect your privacy" >/dev/null
          echo "$POLICY" | grep -F "1. About 13XPlay" >/dev/null
          curl --fail --silent --show-error --head "https://13xplay.upscaleera.com/wp-content/uploads/13xplay-assets/13xplay-logo.webp" >/dev/null
          curl --fail --silent --show-error --head "https://13xplay.upscaleera.com/wp-content/uploads/13xplay-assets/13xplay-hero.webp" >/dev/null
          echo "13Xplay landing, mobile ID card fix, editable footer menu, and Privacy Policy deployed successfully."
